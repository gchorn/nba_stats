version: '3'

services:
  db:
    image: postgres
  web:
    build: .
    command: sh -c "python manage.py migrate && gunicorn -w 2 nba_stats.wsgi:application -b 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db

